services:
  - type: web
    name: rag-service
    env: python
    branch: main
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python rag_service.py
    envVars:
      - key: YANDEX_TOKEN
        sync: false